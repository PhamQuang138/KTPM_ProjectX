# CarHub Garage Overview

Deck 11 slide giới thiệu sản phẩm và kiến trúc CarHub Garage, được xây dựng từ
template `product-launch` của skill `html-ppt`.

Mở `index.html` trong trình duyệt. Dùng phím trái/phải hoặc Space để chuyển
slide, `F` để toàn màn hình và `O` để xem tổng quan.
